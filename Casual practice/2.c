#include <stdio.h>

int multiplyTwoNumbers(int a, int b) {
    return a * b;
}

int main() {
    int result = multiplyTwoNumbers(4, 5);
    printf("The product is %d\n", result);
    return 0;
}

